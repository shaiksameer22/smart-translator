<?xml version="1.0" encoding="utf-8"?>
<!--
    Only the camera-capture temp directory (cacheDir/Images) is exposed via the FileProvider.
    Nothing else of the app's storage is shareable, minimizing what a granted URI can reach.
-->
<paths>
    <cache-path name="my_cache_images" path="Images/" />
</paths>
